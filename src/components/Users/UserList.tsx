import React, { useState } from 'react'

interface POst{
    id: number;
    name:
}
const UserList = () => {
    const [users, setUsers] = useState<User[]>([]); 
    return (
        <div>UserList</div>
    );
}

export default UserList